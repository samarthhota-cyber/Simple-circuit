SimpleCircuit

​A basic logic circuit simulator that runs entirely in your browser.

​What it does:
​Places gates like AND, OR, NAND, XOR, and NOT on a grid canvas.

​Connects pins using a simple click-and-drag wire tool.

​Toggles input switches in real-time to watch signals propagate.

​Saves your circuit setups directly to a local file to load later.

​Runs an automated testbench to verify your circuit's truth table.

​How to run it
1:Save index.html, style.css, and script.js together in one folder.
2:Double-click index.html to open the app instantly in any browser.
